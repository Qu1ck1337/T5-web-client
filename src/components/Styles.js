import styled from 'styled-components'; 
export const Button = styled.div` 
   position: fixed;  
   width: 100%; 
   left: 50vw; 
   bottom: 10vh; 
   height: 20px; 
   font-size: 3rem; 
   cursor: pointer; 
   color: rgba(12, 177, 218, 0.5); 
`